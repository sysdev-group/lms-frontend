// Production environment — used when running ng build --configuration production
export const environment = {
  production: true,
  apiUrl: 'https://your-domain.com/api/v1',  // TODO: Update before deploying
};
